# $schema: https://gitversion.net/schemas/7.0/GitVersion.configuration.json
calculation:
  workflow: GitFlow/v1
  mode: ManualDeployment
  next-version: 7.0.0
  branches:
    main:
      label: alpha
    support:
      label: beta
output: {}
