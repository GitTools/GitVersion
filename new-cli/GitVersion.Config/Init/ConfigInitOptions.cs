using GitVersion.Infrastructure;

namespace GitVersion.Config.Init
{
    [Command("init", "Inits the configuration for current repository.")]
    public class ConfigInitOptions : ConfigOptions
    {
    }
}